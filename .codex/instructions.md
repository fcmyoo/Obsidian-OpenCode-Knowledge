# Codex

Use this repository as a local knowledge base.

## Instructions

- Load `AGENTS.md` for project-level rules.
- Load `vault-template/AGENTS.md` for vault workflows.
- Load `AI_CONFIG.md` for customization.
- Do not modify `raw/`.
- Keep `wiki/index.md` and `wiki/log.md` consistent after changes.
- Prefer relative wiki links.

## MCP

Optional MCP server:
- `scripts/kb_mcp.py` as `project-kb`

Example config is in `.codex/config.toml`.
