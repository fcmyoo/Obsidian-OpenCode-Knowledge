# Claude Code

Use this repository as a local knowledge base.

## Instructions

- Load `AGENTS.md` and `vault-template/AGENTS.md` for knowledge-base workflows.
- Load `AI_CONFIG.md` for user-customizable behavior.
- Do not edit files under `raw/`.
- After any wiki change, update `wiki/index.md` and append `wiki/log.md`.
- Use relative paths for internal wiki links.

## Skills

Prefer skills under `.claude/skills/` and vault skills under `vault-template/.opencode/skill/`.
